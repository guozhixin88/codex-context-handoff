interface:
  display_name: "Context Handoff"
  short_description: "Hand context to a verified thread or worktree."
  default_prompt: "Use $context-handoff to hand this context to the correct verified thread or worktree, then wait for me there."

policy:
  allow_implicit_invocation: true
