interface:
  display_name: "Context Handoff"
  short_description: "Hand minimal context to one verified task owner."
  default_prompt: "Use $context-handoff to hand the five-point task state to a fresh task in the exact verified workspace, then wait there."

policy:
  allow_implicit_invocation: true
