interface:
  display_name: "Context Handoff"
  short_description: "Move long Codex conversations into verified fresh threads."
  default_prompt: "Use $context-handoff to move this conversation into a verified fresh thread and wait for me there."

policy:
  allow_implicit_invocation: true
