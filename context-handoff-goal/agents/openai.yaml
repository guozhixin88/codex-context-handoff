interface:
  display_name: "Context Handoff Goal"
  short_description: "Hand context to the right worktree and start its goal."
  default_prompt: "Use $context-handoff-goal to hand this context to the correct verified thread or worktree and start the concrete goal there."
